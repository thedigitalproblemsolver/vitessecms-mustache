<h1>
    Edit :
    {{adminEditItem.name[]}}
    <div class="float-right">
        {{{adminButtons}}}
    </div>
</h1>
<div class="row">
    <div class="col-xs-12 col-lg-9">
    {{{ adminEditForm }}}
    </div>
    <div class="col-xs-12 col-lg-3" id="layout_editor_fields">
        <h4>%MUSTACHE_AVAILABLE_FIELDS%</h4>
        {{#adminEditItem.availableFields}}
            <a href="#" class="d-block" id="{{_id}}" >{{name[]}}</a>
        {{/adminEditItem.availableFields}}
    </div>
</div>